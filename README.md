# Progressive_Web_App
Se implementó un Service Worker que permite que el CV funcione como una Progressive Web App (PWA), gestionando el caché de recursos para una carga más rápida y soporte sin conexión a internet.
